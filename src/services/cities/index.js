const getCities = () => {
    return [
        {
            id: 1,
            name: "Test city"
        }
    ]
}

const createCity = (data) => {
    // TODO: create city logic

    return data;
}

module.exports = {
    getCities,
    createCity
}